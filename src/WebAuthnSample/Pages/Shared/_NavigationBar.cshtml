<nav class="navbar navbar-expand-lg bg-light fixed-top">
    <div class="container">
        <a href="~/" class="navbar-brand">WebAuthn Sample</a>
        <button class="navbar-toggler" data-bs-target="#navbarContent" data-bs-toggle="collapse">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a href="/" class="nav-link">Home</a>
                </li>
            </ul>
            <ul class="navbar-nav mb-2 mb-lg-0">
                @if (User.Identity is {IsAuthenticated: false })
                {
                    <li class="nav-item">
                        <a asp-page="/Login" asp-route-returnUrl="@Context.Request.Path" class="nav-link">Login</a>
                    </li>
                    <li class="nav-item">
                        <a asp-page="/Register" asp-route-returnUrl="@Context.Request.Path" class="nav-link">Register</a>
                    </li>
                }
                else
                {
                    <li class="nav-item">
                        <a asp-page="/Logout" asp-route-returnUrl="@Context.Request.Path" class="nav-link">Logout</a>
                    </li>
                }
            </ul> 
        </div>
    </div>
</nav>